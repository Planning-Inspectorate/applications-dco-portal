{% extends "views/layouts/main.njk" %}

{% from "govuk/components/notification-banner/macro.njk" import govukNotificationBanner %}
{% from "govuk/components/warning-text/macro.njk" import govukWarningText %}

{% if not title %}
  {% set sectionName = " - " + pageCaption if pageCaption else "" %}
  {% set title %}
    {{ question.pageTitle | default('GOV.UK') }}{{ sectionName }} - Apply for and manage your infrastructure project
  {% endset %}
{% endif %}

{% block pageTitle %}{{ "Error: " + title if errors else title }}{% endblock %}

{% block journey_banner %}
    {% if showJourneyBanner %}
        {{ govukNotificationBanner({
            text: "Another person is working on this section. You may lose any changes you make."
        }) }}
    {% endif %}
{% endblock %}

{# show question content within content, overrides main content #}
{% block content %}
    {{ super() }}
    {% block dynQuestionContent %}{% endblock %}
{% endblock %}

{% if warningMessage %}
  {% set beforeSubmitButton = govukWarningText({
    text: warningMessage
  }) %}
{% endif %}